# Technology logo files

Upload approved logo files here using the filenames listed in `content/partners.json`. Use only logos you are authorised to publish. The current V7 pages use text-based cards until approved files are added.
