const menuButton=document.querySelector("[data-menu]");const menu=document.querySelector(".nav-links");menuButton?.addEventListener("click",()=>{const open=menu.classList.toggle("open");menuButton.setAttribute("aria-expanded",String(open))});document.querySelectorAll("[data-year]").forEach(e=>e.textContent=new Date().getFullYear());const form=document.querySelector("#consultation");form?.addEventListener("submit",async e=>{e.preventDefault();const button=form.querySelector('button[type="submit"]');const status=form.querySelector(".form-status");button.disabled=true;button.textContent="Sending...";status.textContent="";try{const object=Object.fromEntries(new FormData(form));const response=await fetch("https://api.web3forms.com/submit",{method:"POST",headers:{"Content-Type":"application/json","Accept":"application/json"},body:JSON.stringify(object)});const result=await response.json();if(response.status!==200)throw new Error(result.message||"Submission failed");form.reset();status.textContent="Thank you. Your enquiry has been sent successfully.";window.location.href="/thank-you/";if(typeof gtag==="function")gtag("event","generate_lead",{form_name:"website_enquiry"})}catch(error){status.textContent="The form could not be sent. Please call 1300 448 522 or email info@verticsecurity.com.au."}finally{button.disabled=false;button.textContent="Send enquiry"}});
const quickContact=document.querySelector(".floating-contact");const enquiryForm=document.querySelector(".form-wrap");if(quickContact&&enquiryForm){const formObserver=new IntersectionObserver(entries=>{quickContact.classList.toggle("is-hidden",entries.some(entry=>entry.isIntersecting));},{threshold:.1});formObserver.observe(enquiryForm);}
