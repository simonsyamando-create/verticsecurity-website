const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);let nav=$('.nav'),tog=$('.toggle'),menu=$('.menu');addEventListener('scroll',()=>nav?.classList.toggle('scrolled',scrollY>25));tog?.addEventListener('click',()=>menu.classList.toggle('open'));$$('[data-year]').forEach(x=>x.textContent=new Date().getFullYear());let io=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('on')),{threshold:.12});$$('.reveal').forEach(e=>io.observe(e));let co=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting||e.target.dataset.done)return;e.target.dataset.done=1;let end=+e.target.dataset.count,st=performance.now();function r(t){let p=Math.min((t-st)/1200,1);e.target.textContent=Math.round(end*(1-(1-p)**3))+(e.target.dataset.suffix||'');if(p<1)requestAnimationFrame(r)}requestAnimationFrame(r)}),{threshold:.5});$$('[data-count]').forEach(e=>co.observe(e));let c=$('#net');if(c&&!matchMedia('(prefers-reduced-motion: reduce)').matches){let g=c.getContext('2d'),ps=[];function sz(){c.width=innerWidth;c.height=c.parentElement.offsetHeight;ps=Array.from({length:Math.min(85,innerWidth/16)},()=>({x:Math.random()*c.width,y:Math.random()*c.height,vx:(Math.random()-.5)*.25,vy:(Math.random()-.5)*.25}))}function d(){g.clearRect(0,0,c.width,c.height);ps.forEach((p,i)=>{p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>c.width)p.vx*=-1;if(p.y<0||p.y>c.height)p.vy*=-1;g.fillStyle=i%5?'#159fe699':'#53e4cad9';g.beginPath();g.arc(p.x,p.y,i%5?1.5:2.5,0,7);g.fill();for(let j=i+1;j<ps.length;j++){let q=ps[j],z=Math.hypot(p.x-q.x,p.y-q.y);if(z<140){g.strokeStyle=`rgba(50,190,225,${.14*(1-z/140)})`;g.beginPath();g.moveTo(p.x,p.y);g.lineTo(q.x,q.y);g.stroke()}}});requestAnimationFrame(d)}sz();addEventListener('resize',sz);d()}let f=$('#form');f?.addEventListener('submit',e=>{e.preventDefault();let x=new FormData(f),s=encodeURIComponent('Vertic Security enquiry: '+x.get('service')),b=encodeURIComponent(`Name: ${x.get('name')}
Company: ${x.get('company')}
Email: ${x.get('email')}
Phone: ${x.get('phone')}
Service: ${x.get('service')}

Details:
${x.get('message')}`);location.href=`mailto:info@verticsecurity.com.au?subject=${s}&body=${b}`});