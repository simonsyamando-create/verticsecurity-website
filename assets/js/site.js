const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);const nav=$('.nav'),toggle=$('.toggle'),menu=$('.menu');addEventListener('scroll',()=>nav?.classList.toggle('scrolled',scrollY>25));toggle?.addEventListener('click',()=>{menu.classList.toggle('open');toggle.setAttribute('aria-expanded',menu.classList.contains('open'))});$$('[data-year]').forEach(x=>x.textContent=new Date().getFullYear());const io=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('visible')),{threshold:.12});$$('.reveal').forEach(e=>io.observe(e));const co=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting||e.target.dataset.done)return;e.target.dataset.done=1;let end=+e.target.dataset.count,start=performance.now();function go(t){let p=Math.min((t-start)/1300,1),v=Math.round(end*(1-Math.pow(1-p,3)));e.target.textContent=v+(e.target.dataset.suffix||'');if(p<1)requestAnimationFrame(go)}requestAnimationFrame(go)}),{threshold:.55});$$('[data-count]').forEach(e=>co.observe(e));const c=$('#network');if(c&&!matchMedia('(prefers-reduced-motion: reduce)').matches){const g=c.getContext('2d');let pts=[];function size(){c.width=innerWidth;c.height=c.parentElement.offsetHeight;pts=Array.from({length:Math.min(90,Math.floor(innerWidth/15))},()=>({x:Math.random()*c.width,y:Math.random()*c.height,vx:(Math.random()-.5)*.25,vy:(Math.random()-.5)*.25,r:Math.random()*1.8+1}))}function draw(){g.clearRect(0,0,c.width,c.height);for(let i=0;i<pts.length;i++){let p=pts[i];p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>c.width)p.vx*=-1;if(p.y<0||p.y>c.height)p.vy*=-1;g.fillStyle=i%5?'rgba(21,159,230,.58)':'rgba(83,228,202,.9)';g.beginPath();g.arc(p.x,p.y,p.r,0,7);g.fill();for(let j=i+1;j<pts.length;j++){let q=pts[j],d=Math.hypot(p.x-q.x,p.y-q.y);if(d<145){g.strokeStyle=`rgba(70,205,226,${.15*(1-d/145)})`;g.beginPath();g.moveTo(p.x,p.y);g.lineTo(q.x,q.y);g.stroke()}}}requestAnimationFrame(draw)}size();addEventListener('resize',size);draw()}const f=$('#contact-form');f?.addEventListener('submit',e=>{e.preventDefault();let d=new FormData(f),s=encodeURIComponent('Vertic Security enquiry: '+d.get('service')),b=encodeURIComponent(`Name: ${d.get('name')}\nCompany: ${d.get('company')}\nEmail: ${d.get('email')}\nPhone: ${d.get('phone')}\nService: ${d.get('service')}\n\nProject details:\n${d.get('message')}`);location.href=`mailto:info@verticsecurity.com.au?subject=${s}&body=${b}`});