const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);const nav=$('.nav'),toggle=$('.toggle'),menu=$('.menu');addEventListener('scroll',()=>nav?.classList.toggle('scrolled',scrollY>30));toggle?.addEventListener('click',()=>{menu.classList.toggle('open');toggle.setAttribute('aria-expanded',menu.classList.contains('open'))});$$('[data-year]').forEach(x=>x.textContent=new Date().getFullYear());
const reveal=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});$$('.reveal').forEach(e=>reveal.observe(e));
const counters=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting||e.target.dataset.done)return;e.target.dataset.done=1;const end=+e.target.dataset.count,dur=1300,start=performance.now();function run(t){const p=Math.min((t-start)/dur,1),v=Math.round(end*(1-Math.pow(1-p,3)));e.target.textContent=v+(e.target.dataset.suffix||'');if(p<1)requestAnimationFrame(run)}requestAnimationFrame(run)}),{threshold:.6});$$('[data-count]').forEach(e=>counters.observe(e));
const c=$('#network');if(c&&!matchMedia('(prefers-reduced-motion: reduce)').matches){const x=c.getContext('2d');let pts=[];function size(){c.width=innerWidth;c.height=c.parentElement.offsetHeight;pts=Array.from({length:Math.min(75,Math.floor(innerWidth/18))},()=>({x:Math.random()*c.width,y:Math.random()*c.height,vx:(Math.random()-.5)*.22,vy:(Math.random()-.5)*.22}))}function draw(){x.clearRect(0,0,c.width,c.height);pts.forEach((p,i)=>{p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>c.width)p.vx*=-1;if(p.y<0||p.y>c.height)p.vy*=-1;x.fillStyle=i%4?'rgba(21,159,230,.58)':'rgba(92,224,195,.82)';x.beginPath();x.arc(p.x,p.y,i%4?1.6:2.7,0,7);x.fill();for(let j=i+1;j<pts.length;j++){const q=pts[j],d=Math.hypot(p.x-q.x,p.y-q.y);if(d<140){x.strokeStyle=`rgba(70,190,220,${.13*(1-d/140)})`;x.beginPath();x.moveTo(p.x,p.y);x.lineTo(q.x,q.y);x.stroke()}}});requestAnimationFrame(draw)}size();addEventListener('resize',size);draw()}
const f=$('#contact-form');f?.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(f),sub=encodeURIComponent('Vertic Security website enquiry: '+d.get('service')),body=encodeURIComponent(`Name: ${d.get('name')}\nCompany: ${d.get('company')}\nEmail: ${d.get('email')}\nPhone: ${d.get('phone')}\nService: ${d.get('service')}\n\nDetails:\n${d.get('message')}`);location.href=`mailto:info@verticsecurity.com.au?subject=${sub}&body=${body}`});